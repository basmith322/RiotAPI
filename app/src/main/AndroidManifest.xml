<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.riotapi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewUserSummonerProfileActivity"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="Riot API App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SummonerProfileActivity"
            android:label="Summoner Profile" />
        <activity
            android:name=".ChampRotationActivity"
            android:label="Free Champion Rotation" />
        <activity
            android:name=".MatchHistoryActivity"
            android:label="Match History" />
        <activity
            android:name=".ServerActivity"
            android:label="Server Status" />
        <activity
            android:name=".LadderActivity"
            android:label="Solo Queue Ladder" />
        <activity
            android:name=".MenuActivity"
            android:label="Main Menu" />
        <activity
            android:name=".LookupSummonerActivity"
            android:label="Summoner Lookup" />
        <activity
            android:name=".DisplaySummonerActivity"
            android:label="Summoner Results" />
        <activity
            android:name=".MainActivity"
            android:label="Register" />
    </application>

</manifest>